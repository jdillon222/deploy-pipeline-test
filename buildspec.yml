version: 0.2
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file njsSamTemplate --s3-bucket plume-dev-codepipeline
                                   --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  files:
    - njsSamTemplate.yaml
    - outputSamTemplate.yaml
